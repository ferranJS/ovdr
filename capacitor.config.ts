import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.zaaxapp.zaax',
  appName: 'zaax-video-analyzer',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
